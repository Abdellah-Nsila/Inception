services:
  nginx:
    build: ./requirements/nginx
    image: nginx
    container_name: nginx
    ports:
      - 443:443
    networks:
      - inception_network
  
networks:
  inception_network:
    name: inception_network